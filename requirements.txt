# The T-Shirt Brand — Phase 1
# Runtime
Django>=6.1,<6.2
django-environ>=0.14
django-htmx>=1.29
Pillow>=12.0
whitenoise>=6.12
gunicorn>=26.0          # production on Linux
waitress>=3.0; sys_platform == "win32"   # production server on Windows (gunicorn is Unix-only)
Markdown>=3.6            # content pages
django-otp>=1.6          # two-factor sign-in for the console / admin
qrcode>=8.0              # QR code printed by `manage.py otp_setup`

# Integrations (wired up in later milestones)
stripe>=15.0          # Milestone 2 — checkout
httpx>=0.28           # Milestone 3 — Printful API client

# Dormant until Milestone 6 (Cloudflare R2 media storage):
# django-storages[boto3]>=1.14
